import javafx.scene.image.Image;
import javafx.scene.image.ImageView;

public class Player extends ImageView {

    int x;
    int y;
    boolean isDown;

    public Player(Image imageView) {
        super(imageView);
        this.x = x;
        this.y = y;
        this.isDown = isDown;
    }
}
